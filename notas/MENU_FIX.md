He modificado el archivo `src/pages/Home.jsx` y el componente `src/layout/Section.jsx` para asegurar que los enlaces de navegación funcionen correctamente.

Ahora, cada sección tiene un `id` que coincide con el `ScrollLink` de la barra de navegación.

Por favor, intenta ejecutar `npm run dev` de nuevo y verifica si los enlaces del menú ya funcionan.
